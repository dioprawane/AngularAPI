/*const express = require('express');
const { assignmentControllerCreate, assignmentControllerIndex } = require("../controllers/assignmentsController.js");

const routes = express.Router();

routes.post('/a', assignmentControllerCreate);
routes.get('/assignments', assignmentControllerIndex);

module.exports = routes;*/
